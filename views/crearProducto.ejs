<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Crear Nuevo Producto</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    rel="stylesheet"
  />
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center p-6">
  <div class="bg-white rounded-lg shadow-lg p-8 w-full max-w-lg">
    <h1 class="text-3xl font-bold mb-6 text-center text-gray-800">Crear Nuevo Producto</h1>
    
    <form method="POST" action="/producto/nuevo" class="space-y-5">
      <div>
        <label for="nombre" class="block mb-1 font-semibold text-gray-700">Nombre</label>
        <input
          type="text"
          name="nombre"
          id="nombre"
          required
          class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
        />
      </div>

      <div>
        <label for="descripcion" class="block mb-1 font-semibold text-gray-700">Descripción</label>
        <textarea
          name="descripcion"
          id="descripcion"
          rows="3"
          class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
          placeholder="Descripción del producto"
        ></textarea>
      </div>

      <div class="grid grid-cols-2 gap-4">
        <div>
          <label for="categoria" class="block mb-1 font-semibold text-gray-700">Categoría</label>
          <input
            type="text"
            name="categoria"
            id="categoria"
            class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
          />
        </div>
        <div>
          <label for="subcategoria" class="block mb-1 font-semibold text-gray-700">Subcategoría</label>
          <input
            type="text"
            name="subcategoria"
            id="subcategoria"
            class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
          />
        </div>
      </div>

      <div>
        <label for="id_marca" class="block mb-1 font-semibold text-gray-700">Marca existente</label>
        <select
          name="id_marca"
          id="id_marca"
          class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
        >
          <option value="">-- Seleccione una marca --</option>
          <% marcas.forEach(marca => { %>
            <option value="<%= marca.id_marca %>"><%= marca.nombre %></option>
          <% }); %>
        </select>
      </div>

      <div>
        <label for="nueva_marca" class="block mb-1 font-semibold text-gray-700">
          O crear nueva marca
        </label>
        <input
          type="text"
          name="nueva_marca"
          id="nueva_marca"
          placeholder="Nombre de nueva marca"
          class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
        />
      </div>

      <div class="grid grid-cols-2 gap-4">
        <div>
          <label for="precio" class="block mb-1 font-semibold text-gray-700">Precio</label>
          <input
            type="number"
            name="precio"
            id="precio"
            required
            min="0"
            class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
          />
        </div>
        <div>
          <label for="stock" class="block mb-1 font-semibold text-gray-700">Stock inicial</label>
          <input
            type="number"
            name="stock"
            id="stock"
            required
            min="0"
            class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
          />
        </div>
      </div>

      <div>
        <label for="id_sucursal" class="block mb-1 font-semibold text-gray-700">Sucursal</label>
        <select
          name="id_sucursal"
          id="id_sucursal"
          required
          class="w-full px-4 py-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500"
        >
          <option value="1">Sucursal 1</option>
          <option value="2">Sucursal 2</option>
          <!-- Puedes cargar dinámicamente sucursales desde la base de datos -->
        </select>
      </div>

      <div class="text-center">
        <button
          type="submit"
          class="bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-md transition-colors flex items-center justify-center mx-auto gap-2"
        >
          <i class="fas fa-check"></i> Crear Producto
        </button>
      </div>
    </form>

    <div class="mt-6 text-center">
      <a href="/tienda" class="text-blue-600 hover:text-blue-800 transition-colors inline-flex items-center">
        <i class="fas fa-arrow-left mr-2"></i> Volver a la tienda
      </a>
    </div>
  </div>
</body>
</html>
